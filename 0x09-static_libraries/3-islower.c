#include "main.h"
/**
 * _islower - checks if a given character is lowecase or not
 * @c: the character being checked.
 *
 * Return: 1 if lowercase 0 if not.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
